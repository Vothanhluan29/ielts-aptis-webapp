import React from 'react';
import ListeningAptisResultPage from '../../../../public/pages/APTIS/listening/ListeningAptisResultPage';

/**
 * Admin View-Only Listening Submission Detail
 * Nhúng trực tiếp trang Result của student để admin xem chi tiết lỗi sai và bài làm.
 */
const ListeningSubmissionViewPage = () => {
  return <ListeningAptisResultPage />;
};

export default ListeningSubmissionViewPage;
